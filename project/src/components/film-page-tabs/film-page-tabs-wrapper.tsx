import { FC, PropsWithChildren } from 'react';

const FilmPageTabsWrapper: FC<PropsWithChildren> = ({ children }) => (
  <div className="film-card__desc">
    children
  </div>
);

export default FilmPageTabsWrapper;
